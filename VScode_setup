download for mac
extentions: 
  git graph
  intellicode
  python/pylance
  jupyter notebook renderers
  jupyter

vs code settings:
  view>appearance>panel - right click>panel position> right
  keyboard shortcuts - run selection in interactive window (ctrl + r)
  keyboard shortcuts - acceptSelectedSuggestion > (tab)

viewing variable assignments:
  install jupyter extention
  go to keyboard shortcuts > edit "wehn" expression to remove "&& jupyter.ownsSelection"

set the environment:
  cmd+shift+p > python: select interpreter


